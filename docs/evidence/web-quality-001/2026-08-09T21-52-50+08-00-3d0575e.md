# Evidence: WEB-QUALITY-001 — Scope-Corrected Executor Verification

| Field | Value |
|---|---|
| Run fingerprint | `WEB-QUALITY-001/3d0575e/2026-08-09T21:52:50+08:00` |
| Target commit SHA | `3d0575e8dbd6692abc4c5404e4a64524f5b99585` |
| Evidence grade | Executor-recorded |
| Remote verification | `origin/main` resolved to the target commit SHA. |
| Gate conclusion | Executor exit criteria met again; independent Quality Agent review is still required. |

## Scope correction

The prior `9e413f6` review identified that Button's non-link branch no longer propagated `className` and `variant` exactly as it had before. This target restores the original prop-spread ordering and only changes the unused binding to an explicitly discarded `omittedHref` value. No human risk acceptance was needed because the behavior change was removed.

## Commands and results

| Method | Result |
|---|---|
| `npm run lint` | Passed (exit `0`). |
| `npm run build` | Passed; TypeScript and all 16 static pages completed. |
| Built 404 inspection | Passed: `out/404.html` and `out/404/index.html` contain Home with href `/zh/`. |
| Baseline Button comparison | Passed: compared against pre-task `735d3a4`; only formatting, the renamed discarded binding and its explanatory comment differ. |

## Residuals

- Await independent Quality Agent verification of this exact target SHA.
- No Product, Architecture, Quality or Release acceptance is claimed.
