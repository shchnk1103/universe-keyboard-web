# Evidence: WEB-QUALITY-001 — Executor Verification

| Field | Value |
|---|---|
| Run fingerprint | `WEB-QUALITY-001/9e413f6/2026-08-09T21:46:09+08:00` |
| Target commit SHA | `9e413f6e446d44958046d4e68a8bbd773db7dad7` |
| Evidence grade | Executor-recorded |
| Remote verification | `origin/main` resolved to the target commit SHA. |
| Scope | `src/app/not-found.tsx`, `src/components/ui/Button.tsx`, and KOS Assignment/status documents only. |
| Gate conclusion | Executor exit criteria met. This is not a human Quality / Release Gate conclusion. |

## Code changes

1. The 404 page now imports Next.js `Link` and keeps its internal Home destination as `/zh/`.
2. `Button` now separates link and button props before spreading them, eliminating the unused `href` binding while preserving its public variants, children, classes and link behavior.

## Commands and results

| Method | Result |
|---|---|
| `npm run lint` | Passed (exit `0`). |
| `npm run build` | Passed (static export and TypeScript completed). An initial sandboxed attempt could not fetch Google Fonts; the same build passed when rerun with network access. |
| Static 404 navigation | Passed against the built `out/` files served locally: `/404/` exposed `Home` with href `/zh/`; clicking it reached `/zh/`, where the Chinese homepage heading was visible. |

## Environment note

The Next.js development server cannot render an arbitrary unknown path with this static-export locale configuration; it emitted a missing `[locale]` parameter error. This was not used as product evidence. The built static 404 page was tested instead and passed. Hosted deep-link behavior remains a future release Gate requirement.

## Residuals

- `WEB-QUALITY-001` remains open until a non-executing Quality Agent independently reviews this exact SHA and records a `Quality-reverified` result.
- No Product, Architecture, Quality or Release acceptance is claimed.
